{
  "name": "pixel-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev ./public --binding",
    "deploy": "wrangler pages deploy ./public --project-name=pixel-store"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
